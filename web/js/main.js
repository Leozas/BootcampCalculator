//structure = var, then defined func, then overall func

/*
divApp.appendChild( newrow(),
                    newrow(
                        newcol(
                            newrow(outputfield)
                            newrow(inputfield(form))
                            newrow(
                                newcol(), newcol(),newcol()
                            newrow(
                                newcol(), newcol(),newcol()
                            newrow(
                                newcol(), newcol(),newcol()
                            )
                        newcol(
                            newrow(
                                newbtn(edgecases as modal)
                            )
                        )
                    newrow()
*/